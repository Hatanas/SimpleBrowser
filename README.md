# SimpleBrowser

現在製作中のGameLauncherに使う予定の，HTMLファイルを読み込んで表示するだけの簡単なブラウザです．
ScalaFXのWebViewを使っているので，JavaScriptも実行できます．

## 実行環境
最新のJREがあれば実行できます．

## 使い方
普通にjarファイルを実行して下さい．
```sh
java -jar SimpleBrowser-X.X.X.jar HTMLファイルのパス
```
`HTMLファイルのパス`には，jarファイルのあるディレクトリからの相対パスを指定して下さい．
